spring:
  rsocket:
    server:
      port: 7071

mqtt:
  enabled: false

internal:
  subscriber:
    retry-attempts: 1
    retry-delay-millis: 100

description:
  default-resource: 'test_sensors.json'

logging:
  level:
    com.github.ngnhub.iot_device_simulator: debug
